name: test trigger
on:
  workflow_dispatch:

jobs:
  job1:
    uses: ./.github/workflows/auto-build.yml

  job2:
    needs: job1

    runs-on: ubuntu-latest

    steps:

      - run: echo "${{ jobs.job1.outputs.build_result }}"
